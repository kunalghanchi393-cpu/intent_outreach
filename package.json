{
  "name": "intent-driven-cold-outreach-agent",
  "version": "1.0.0",
  "description": "A production-ready AI system that generates personalized, human-sounding outreach messages through a structured 7-step reasoning workflow",
  "main": "dist/index.js",
  "bin": {
    "outreach-agent": "dist/server.js"
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:prod": "npm run lint && npm run test && npm run build",
    "start": "node dist/server.js",
    "start:dev": "ts-node src/server.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write src/**/*.ts",
    "clean": "rimraf dist"
  },
  "keywords": [
    "ai",
    "outreach",
    "sales",
    "typescript",
    "property-based-testing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.19.37",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "fast-check": "^3.15.0",
    "jest": "^29.5.0",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
